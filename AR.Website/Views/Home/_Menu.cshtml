@model AR.Website.Models.MenuModel
@using AR.Domain.Identity

<div class="navbar navbar-inverse navbar-fixed-top">
    <div class="container">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <img src="@Url.Content("~/Content/Images/Logo36x35.png")" class="logo-small pull-left" />
            @Html.Link().HomeActionLink(x => x.Index()).Text("FFLAH").Class("navbar-brand")
</div>
        <div class="navbar-collapse collapse">
            @{
                var menuNav = Html.BootstrapNav()
                    .AsNavBar()
                    .Class("navbar-left")
                    .AddElement(
                        Html.BootstrapNavItem().AddElement(
                            Html.Link().ActionLink<HomeController>(x => x.Index()).Text("Home")
                        )
                    );

                if (Model.UserRoles.Any(x => x == IdentityRole.MasterAdmin))
                {
                    menuNav.AddElement(
                        Html.BootstrapNavItem().AddElement(
                            Html.Link().ActionLink<ErrorLogController>(x => x.Index()).Text("Error Log")
                        )
                    );

                    var dropdownMaintenance = Html.BootstrapNavItem()
                        .AsDropdown("Maintenance")
                        .AddElement(
                            Html.BootstrapNav()
                                .AsDropdown()
                                .AddElement(
                                    Html.BootstrapNavItem().AddElement(
                                        Html.Link().ActionLink<DataMaintenanceController>(x => x.Dashboard()).Text("Dashboard")
                                    )
                                )
                            );
                    menuNav.AddElement(dropdownMaintenance);
                }

            }
            @menuNav

            @{
                var loginNav = Html.BootstrapNav()
                    .AsNavBar()
                    .Class("navbar-right");

                if (Model.IsAuthenticated)
                {
                    loginNav.AddElement(
                        Html.BootstrapNavItem().AddElement(
                        Html.Link().ActionLink<AccountController>(x => x.LogOut()).Text("Logout")
                        )
                    );
                }
                else
                {
                    loginNav.AddElement(
                        Html.BootstrapNavItem().AddElement(
                        Html.Link().ActionLink<AccountController>(x => x.Login(null)).Text("Login")
                        )
                    );
                }
            }
            @loginNav
        </div>
    </div>
</div>
